#include<stdio.h>
int main() {
    int a = 5;
    int b=3;
    int c=a+b;// +,= is operators and a,b is operands and c is result
    printf("The value of a is %d\n and value of b is %d\n is sum of a and b is %d\n",a,b,c);
    printf("The remainder when a is divided by b is: %d\n",a%b);
    
    return 0;
}