#include <stdio.h>

int main() {
    int r = 5; // Declare and initialize 'r' with '5'
    float area = 3.14 * r * r;
    printf("The area of the circle with radius %d is %.2f\n", r, area);
    return 0;
}